
import { Box, Button } from "@mui/material";
import styled from "styled-components";

const StyledButtonPagina = styled(Button)`
    && {
       width: 15vw;
       margin-top: 0.5rem;
       border: 1px solid white;
       color: white;
}`;


export default StyledButtonPagina;